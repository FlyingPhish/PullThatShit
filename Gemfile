source 'https://rubygems.org'

# Main Ones
gem 'colorize'
gem 'tty-command'
gem 'tty-prompt'

# Development Ones
# Change the '.bundle/config' file to BUNDLE_WITH: "dev" if you want those gems.
group :dev do
  gem 'rubocop'
  gem 'solargraph'
end
